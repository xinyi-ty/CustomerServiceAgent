# ====================
# Web 框架核心
# ====================
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6          # 处理文件上传

# ====================
# 数据验证与配置
# ====================
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# ====================
# HTTP 客户端（调用大模型 API）
# ====================
httpx==0.25.1                     # 异步 HTTP 请求
requests==2.31.0                  # 同步 HTTP 请求（备用）

# ====================
# 数据库
# ====================
# SQLite3 是 Python 内置，不需要安装
# 如需更高级的 ORM，可取消下面注释
# sqlalchemy==2.0.23

# ====================
# 多模态同学需要（OCR + 视频处理）
# ====================
paddlepaddle==2.6.0               # PaddleOCR 依赖（CPU 版本）
paddleocr==2.7.3                  # OCR 识别
opencv-python==4.8.1.78           # 视频抽帧处理
Pillow==10.1.0                    # 图片处理

# ====================
# RAG 同学可能需要（后期升级）
# ====================
# chromadb==0.4.22                # 向量数据库
# sentence-transformers==2.2.2   # 嵌入模型

# ====================
# 工具类
# ====================
python-dateutil==2.8.2            # 时间处理
uuid6==2023.12.1                  # 生成工单 ID（Python 内置也可用）

# ====================
# 可选：本地测试用
# ====================
# pytest==7.4.3                   # 单元测试
# pytest-asyncio==0.21.1          # 异步测试
